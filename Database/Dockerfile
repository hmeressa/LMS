FROM postgres:latest

ENV POSTGRES_USER ${DOCKER_POSTGRES_USER}
ENV POSTGRES_PASSWORD ${DOCKER_POSTGRES_PASSWORD}
ENV POSTGRES_DB ${DOCKER_POSTGRES_DB}

COPY migration.sql /docker-entrypoint-initdb.d/

EXPOSE 5432
